"use client";

import { useEffect, useMemo, useRef, useState, useTransition } from "react";
import { createEwcEntry, deleteEwcEntry, reorderEwcEntries, updateEwcEntry, updateEwcMoved, type EwcEntryType } from "./actions";

export type EwcEntry = {
  id: number; section: EwcEntryType; client_name: string; service_date: string | null; service_type: string; amount_owed: number; amount_paid: number; stripe_fee: number; date_paid: string | null; notes: string | null; moved: boolean; sort_order: number; created_at: string; updated_at: string;
};
type TextField = "client_name" | "service_date" | "service_type" | "date_paid" | "notes";
type MoneyField = "amount_owed" | "amount_paid" | "stripe_fee";
function money(value:number){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",minimumFractionDigits:2,maximumFractionDigits:2}).format(Number.isFinite(value)?value:0);}
function parseMoney(value:string){const parsed=Number(value.replace(/[^0-9.-]/g,""));return Number.isFinite(parsed)?parsed:0;}
function getOutstanding(row:EwcEntry){return Math.max(Number(row.amount_owed||0)-Number(row.amount_paid||0),0);}
function MoneyInput({value,onCommit}:{value:number;onCommit:(value:number)=>void}){const[draft,setDraft]=useState(Number(value||0).toFixed(2));useEffect(()=>setDraft(Number(value||0).toFixed(2)),[value]);return <input value={draft} inputMode="decimal" onFocus={e=>e.currentTarget.select()} onChange={e=>setDraft(e.target.value.replace(/[^0-9.-]/g,""))} onBlur={()=>{const parsed=parseMoney(draft);setDraft(parsed.toFixed(2));onCommit(parsed);}} className="h-full w-full border-0 bg-transparent px-3 py-2.5 text-right text-sm text-[#243128] outline-none focus:bg-white focus:shadow-[inset_0_0_0_2px_rgba(100,125,91,0.24)]"/>;}

export default function EwcTracker({initialEntries}:{initialEntries:EwcEntry[]}){
 const[entries,setEntries]=useState(initialEntries);const[dragged,setDragged]=useState<{section:EwcEntryType;id:number}|null>(null);const[savedMessage,setSavedMessage]=useState("");const[isPending,startTransition]=useTransition();const saveTimer=useRef<ReturnType<typeof setTimeout>|null>(null);
 const bySection=(section:EwcEntryType)=>entries.filter(e=>e.section===section).sort((a,b)=>a.sort_order-b.sort_order);
 const sessions=useMemo(()=>bySection("Session"),[entries]);const linkedin=useMemo(()=>bySection("LinkedIn"),[entries]);const other=useMemo(()=>bySection("Other"),[entries]);
 const totals=useMemo(()=>{const billable=entries.filter(r=>r.section!=="LinkedIn");const owed=billable.reduce((s,r)=>s+Number(r.amount_owed||0),0);const paidTowardOwed=billable.reduce((s,r)=>s+Math.min(Number(r.amount_paid||0),Number(r.amount_owed||0)),0);const extraPaid=billable.reduce((s,r)=>s+Math.max(Number(r.amount_paid||0)-Number(r.amount_owed||0),0),0);const linkedInGross=entries.filter(r=>r.section==="LinkedIn").reduce((s,r)=>s+Number(r.amount_paid||0),0);const stripe=entries.reduce((s,r)=>s+Number(r.stripe_fee||0),0);const grossReceived=entries.reduce((s,r)=>s+Number(r.amount_paid||0),0);return{owed,paidTowardOwed,extraPaid,linkedInGross,received:grossReceived-stripe,outstanding:billable.reduce((s,r)=>s+getOutstanding(r),0)};},[entries]);
 function flashSaved(message="Saved to Supabase"){setSavedMessage(message);if(saveTimer.current)clearTimeout(saveTimer.current);saveTimer.current=setTimeout(()=>setSavedMessage(""),1600);}
 function updateLocal(id:number,field:TextField|MoneyField|"moved",value:string|number|boolean){setEntries(c=>c.map(r=>r.id===id?{...r,[field]:value}:r));}
 function persistRow(row:EwcEntry,overrides:Partial<EwcEntry>={}){const n={...row,...overrides};const f=new FormData();f.set("id",String(n.id));f.set("client_name",n.client_name);f.set("service_date",n.service_date??"");f.set("service_type",n.service_type);f.set("amount_owed",String(n.amount_owed??0));f.set("amount_paid",String(n.amount_paid??0));f.set("stripe_fee",String(n.stripe_fee??0));f.set("date_paid",n.date_paid??"");f.set("notes",n.notes??"");startTransition(async()=>{try{await updateEwcEntry(f);flashSaved();}catch(e){console.error(e);flashSaved("Could not save");}});}
 function toggleMoved(row:EwcEntry,checked:boolean){updateLocal(row.id,"moved",checked);startTransition(async()=>{try{await updateEwcMoved(row.id,checked);flashSaved("Moved saved");}catch(e){console.error(e);updateLocal(row.id,"moved",row.moved);flashSaved("Could not save");}});}
 function addRow(section:EwcEntryType){startTransition(async()=>{try{const row=await createEwcEntry(section) as EwcEntry;setEntries(c=>[row,...c]);flashSaved(`${section} row added`);}catch(e){console.error(e);flashSaved("Could not add row");}});}
 function removeRow(id:number){startTransition(async()=>{try{await deleteEwcEntry(id);setEntries(c=>c.filter(r=>r.id!==id));flashSaved("Row deleted");}catch(e){console.error(e);flashSaved("Could not delete");}});}
 function reorderWithinSection(section:EwcEntryType,targetId:number){if(!dragged||dragged.section!==section||dragged.id===targetId)return;const rows=bySection(section);const from=rows.findIndex(r=>r.id===dragged.id),to=rows.findIndex(r=>r.id===targetId);if(from<0||to<0)return;const reordered=[...rows];const[m]=reordered.splice(from,1);reordered.splice(to,0,m);const map=new Map(reordered.map((r,i)=>[r.id,i+1]));setEntries(c=>c.map(r=>r.section===section?{...r,sort_order:map.get(r.id)??r.sort_order}:r));setDragged(null);startTransition(async()=>{try{await reorderEwcEntries(section,reordered.map(r=>r.id));flashSaved("Order saved");}catch(e){console.error(e);flashSaved("Could not reorder");}});}
 const inputClass="h-full w-full border-0 bg-transparent px-3 py-2.5 text-sm text-[#243128] outline-none focus:bg-white focus:shadow-[inset_0_0_0_2px_rgba(100,125,91,0.24)]";
 function StandardTable({section,rows}:{section:"Session"|"Other";rows:EwcEntry[]}){return <section className="overflow-hidden rounded-2xl border border-[#dfe6db] bg-white shadow-sm"><div className="flex items-center justify-between border-b border-[#dfe6db] bg-[#fbfaf6] px-5 py-4"><div><h2 className="text-xl font-bold text-[#243128]">{section==="Session"?"Sessions":"Other"}</h2><p className="mt-1 text-sm text-[#708075]">Newest entries stay at the top. All edits auto-save.</p></div><button type="button" onClick={()=>addRow(section)} disabled={isPending} className="rounded-xl bg-[#647d5b] px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">+ Add {section==="Session"?"Session":"Other"}</button></div><div className="overflow-x-auto"><div className="min-w-[1260px]"><div className="grid grid-cols-[44px_220px_130px_180px_120px_120px_130px_1fr_80px_44px] border-b border-[#dfe6db] bg-[#eef2ea] text-[10px] font-bold uppercase tracking-[0.1em] text-[#647066]">{['#','Client','Date','Service','Owed','Paid','Outstanding','Notes','Moved',''].map((l,i)=><div key={`${l}-${i}`} className="border-r border-[#dfe6db] px-3 py-3 text-center">{l}</div>)}</div>{rows.length===0?<div className="p-10 text-center text-sm text-[#708075]">No entries yet.</div>:null}{rows.map((row,index)=><div key={row.id} onDragOver={e=>e.preventDefault()} onDrop={()=>reorderWithinSection(section,row.id)} className={`group grid grid-cols-[44px_220px_130px_180px_120px_120px_130px_1fr_80px_44px] border-b border-[#edf0ea] ${index%2?'bg-[#fcfdfb]':'bg-white'}`}><button type="button" draggable onDragStart={()=>setDragged({section,id:row.id})} className="cursor-grab border-r border-[#edf0ea] text-[#a5aea6]">⋮⋮</button><input value={row.client_name} onChange={e=>updateLocal(row.id,'client_name',e.target.value)} onBlur={()=>persistRow(row)} placeholder="Client name" className={`${inputClass} border-r border-[#edf0ea]`}/><input type="date" value={row.service_date??''} onChange={e=>{updateLocal(row.id,'service_date',e.target.value);persistRow(row,{service_date:e.target.value});}} className={`${inputClass} border-r border-[#edf0ea]`}/><input value={row.service_type} onChange={e=>updateLocal(row.id,'service_type',e.target.value)} onBlur={()=>persistRow(row)} placeholder="Service" className={`${inputClass} border-r border-[#edf0ea]`}/><MoneyInput value={row.amount_owed} onCommit={v=>{updateLocal(row.id,'amount_owed',v);persistRow(row,{amount_owed:v});}}/><MoneyInput value={row.amount_paid} onCommit={v=>{updateLocal(row.id,'amount_paid',v);persistRow(row,{amount_paid:v});}}/><div className="flex items-center justify-end border-r border-[#edf0ea] bg-[#fbf6f3] px-3 text-sm font-semibold text-[#9a554d]">{money(getOutstanding(row))}</div><input value={row.notes??''} onChange={e=>updateLocal(row.id,'notes',e.target.value)} onBlur={()=>persistRow(row)} placeholder="Notes" className={`${inputClass} border-r border-[#edf0ea]`}/><div className="flex items-center justify-center border-r border-[#edf0ea]"><input type="checkbox" checked={Boolean(row.moved)} onChange={e=>toggleMoved(row,e.target.checked)} className="h-5 w-5 cursor-pointer accent-[#647d5b]"/></div><button type="button" onClick={()=>removeRow(row.id)} className="text-[#a45f58] hover:bg-[#fbefed]">×</button></div>)}</div></div></section>;}
 function LinkedInTable({rows}:{rows:EwcEntry[]}){return <section className="overflow-hidden rounded-2xl border border-[#dfe6db] bg-white shadow-sm"><div className="flex items-center justify-between border-b border-[#dfe6db] bg-[#fbfaf6] px-5 py-4"><div><h2 className="text-xl font-bold text-[#243128]">LinkedIn</h2><p className="mt-1 text-sm text-[#708075]">Newest entries stay at the top. All edits auto-save.</p></div><button type="button" onClick={()=>addRow("LinkedIn")} disabled={isPending} className="rounded-xl bg-[#647d5b] px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">+ Add LinkedIn Client</button></div><div className="overflow-x-auto"><div className="min-w-[1120px]"><div className="grid grid-cols-[44px_220px_130px_190px_125px_125px_145px_1fr_80px_44px] border-b border-[#dfe6db] bg-[#eef2ea] text-[10px] font-bold uppercase tracking-[0.1em] text-[#647066]">{['#','Client','Date','Service','Paid','Stripe Fee','Total Received','Notes','Moved',''].map((l,i)=><div key={`${l}-${i}`} className="border-r border-[#dfe6db] px-3 py-3 text-center">{l}</div>)}</div>{rows.length===0?<div className="p-10 text-center text-sm text-[#708075]">No LinkedIn entries yet.</div>:null}{rows.map((row,index)=><div key={row.id} onDragOver={e=>e.preventDefault()} onDrop={()=>reorderWithinSection("LinkedIn",row.id)} className={`group grid grid-cols-[44px_220px_130px_190px_125px_125px_145px_1fr_80px_44px] border-b border-[#edf0ea] ${index%2?'bg-[#fcfdfb]':'bg-white'}`}><button type="button" draggable onDragStart={()=>setDragged({section:"LinkedIn",id:row.id})} className="cursor-grab border-r border-[#edf0ea] text-[#a5aea6]">⋮⋮</button><input value={row.client_name} onChange={e=>updateLocal(row.id,'client_name',e.target.value)} onBlur={()=>persistRow(row)} placeholder="Client name" className={`${inputClass} border-r border-[#edf0ea]`}/><input type="date" value={row.service_date??''} onChange={e=>{updateLocal(row.id,'service_date',e.target.value);persistRow(row,{service_date:e.target.value});}} className={`${inputClass} border-r border-[#edf0ea]`}/><input value={row.service_type} onChange={e=>updateLocal(row.id,'service_type',e.target.value)} onBlur={()=>persistRow(row)} placeholder="LinkedIn service" className={`${inputClass} border-r border-[#edf0ea]`}/><MoneyInput value={row.amount_paid} onCommit={v=>{updateLocal(row.id,'amount_paid',v);persistRow(row,{amount_paid:v});}}/><MoneyInput value={row.stripe_fee} onCommit={v=>{updateLocal(row.id,'stripe_fee',v);persistRow(row,{stripe_fee:v});}}/><div className="flex items-center justify-end border-r border-[#edf0ea] bg-[#f5f8f2] px-3 text-sm font-bold text-[#56754f]">{money(Number(row.amount_paid||0)-Number(row.stripe_fee||0))}</div><input value={row.notes??''} onChange={e=>updateLocal(row.id,'notes',e.target.value)} onBlur={()=>persistRow(row)} placeholder="Notes" className={`${inputClass} border-r border-[#edf0ea]`}/><div className="flex items-center justify-center border-r border-[#edf0ea]"><input type="checkbox" checked={Boolean(row.moved)} onChange={e=>toggleMoved(row,e.target.checked)} className="h-5 w-5 cursor-pointer accent-[#647d5b]"/></div><button type="button" onClick={()=>removeRow(row.id)} className="text-[#a45f58] hover:bg-[#fbefed]">×</button></div>)}</div></div></section>;}
 return <section className="min-w-0 flex-1 bg-[#f7f8f3] text-[#243128]"><header className="border-b border-[#dfe6db] bg-[#fbfaf6] px-6 py-7 lg:px-10"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#7f9975]">Emily Weiss Consulting</p><h1 className="mt-2 text-3xl font-bold tracking-tight">EWC</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#708075]">Track Sessions, LinkedIn work, and Other EWC clients in one place.</p></header><div className="space-y-7 p-6 lg:p-10"><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><div className="rounded-2xl border border-[#dfe6db] bg-white p-5 shadow-sm"><p className="text-sm font-medium text-[#708075]">Total Owed</p><p className="mt-3 text-3xl font-bold">{money(totals.owed)}</p></div><div className="rounded-2xl border border-[#dfe6db] bg-white p-5 shadow-sm"><p className="text-sm font-medium text-[#708075]">Paid Toward Owed</p><p className="mt-3 text-3xl font-bold text-[#56754f]">{money(totals.paidTowardOwed)}</p>{totals.extraPaid>0?<p className="mt-1 text-xs text-[#708075]">+ {money(totals.extraPaid)} extra paid</p>:null}</div><div className="rounded-2xl border border-[#dfe6db] bg-white p-5 shadow-sm"><p className="text-sm font-medium text-[#708075]">Net Received</p><p className="mt-3 text-3xl font-bold text-[#56754f]">{money(totals.received)}</p><p className="mt-1 text-xs text-[#708075]">Includes {money(totals.linkedInGross)} LinkedIn gross</p></div><div className="rounded-2xl border border-[#ead4d0] bg-[#fffdfc] p-5 shadow-sm"><p className="text-sm font-medium text-[#8b6a65]">Outstanding</p><p className="mt-3 text-3xl font-bold text-[#9a554d]">{money(totals.outstanding)}</p></div></section><div className="flex items-center justify-between rounded-xl border border-[#dfe6db] bg-white px-4 py-3 text-xs text-[#708075]"><span>Everything auto-saves to Supabase. Moved saves immediately when checked.</span><span className="font-semibold text-[#647d5b]">{isPending?"Saving...":savedMessage||"Auto-saved"}</span></div><StandardTable section="Session" rows={sessions}/><LinkedInTable rows={linkedin}/><StandardTable section="Other" rows={other}/></div></section>;
}