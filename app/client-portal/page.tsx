import Link from "next/link";import {createClient} from "@/lib/supabase-server";import {claimClientPortal} from "./actions";import {redirect} from "next/navigation";
export const dynamic="force-dynamic";
export default async function Page(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:client}=await supabase.from("clients").select("id,name,portal_user_id").eq("portal_user_id",user.id).maybeSingle();if(!client)return <main className="mx-auto max-w-xl px-5 py-16"><div className="rounded-3xl border bg-white p-8"><h1 className="font-serif text-3xl">Connect your client portal</h1><p className="mt-3 text-sm leading-6 text-[#708075]">We found your JGO Hire login. Connect it to your client record to open your private documents, resources, and job tracker.</p><form action={claimClientPortal}><button className="mt-6 rounded-xl bg-[#647d5b] px-5 py-3 text-sm font-bold text-white">Connect My Portal</button></form></div></main>;const first=String(client.name||"there").split(" ")[0];const cards=[['My Documents','Your resume, cover letters, reports, and files shared by JGO Hire.','/client-portal/documents'],['Job Tracker','A simple place to keep every application, next step, and interview organized.','/client-portal/jobs'],['Resources','Your JGO Hire guides, templates, and career resources.','/client-portal/resources']];return <main className="mx-auto max-w-6xl px-5 py-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#7f9975]">Client Portal</p><h1 className="mt-2 font-serif text-4xl">Hi, {first}.</h1><p className="mt-2 text-[#708075]">Everything for your job search, in one place.</p><div className="mt-8 grid gap-5 md:grid-cols-3">{cards.map(([t,d,h])=><Link key={t} href={h} className="rounded-2xl border border-[#dfe6db] bg-white p-6 transition hover:-translate-y-0.5 hover:shadow-md"><h2 className="text-xl font-bold">{t}</h2><p className="mt-2 text-sm leading-6 text-[#708075]">{d}</p><p className="mt-6 text-sm font-bold text-[#647d5b]">Open →</p></Link>)}</div></main>}
