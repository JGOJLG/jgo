export function getClientInitials(name:string|null|undefined){const clean=(name||"").replace(/\([^)]*\)/g," ").replace(/[^A-Za-z' -]/g," ").trim();const parts=clean.split(/\s+/).filter(Boolean);if(!parts.length)return"C";const first=parts[0]?.match(/[A-Za-z]/)?.[0]||"C";const last=parts.length>1?parts[parts.length-1]?.match(/[A-Za-z]/)?.[0]:"";return`${first}${last||""}`.toUpperCase()}
export default function ClientAvatar({name,linkedinUrl}:{name:string|null;linkedinUrl?:string|null}){return <div className="flex items-center gap-2"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-[#e8eee3] text-sm font-bold text-[#4d6247]">{getClientInitials(name)}</div>{linkedinUrl?<a href={linkedinUrl} target="_blank" rel="noreferrer" className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#0A66C2] text-[11px] font-black text-white shadow-sm transition hover:-translate-y-0.5" title="Open LinkedIn profile" aria-label="Open LinkedIn profile">in</a>:null}</div>}
