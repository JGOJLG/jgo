"use client";
import{useState}from"react";
type Props={clientId:number;clientName:string;email:string|null;linked:boolean;invitedAt:string|null};
export default function ClientPortalAccess({clientId,email,linked,invitedAt}:Props){const[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[invited,setInvited]=useState(Boolean(invitedAt));async function invite(){if(!email){setMessage("Add an email address first.");return}setBusy(true);setMessage("");try{const r=await fetch("/api/client-portal/invite",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({clientId})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to send invite.");setInvited(true);setMessage(d.message||"Invite sent.")}catch(e){setMessage(e instanceof Error?e.message:"Unable to send invite.")}finally{setBusy(false)}}return <div className="mt-3 flex flex-wrap items-center gap-3"><button type="button" onClick={invite} disabled={busy||linked||!email} className="rounded-full border border-[#cbd8c4] bg-white px-4 py-2 text-xs font-bold text-[#4d6247] shadow-sm transition hover:bg-[#f4f7f1] disabled:cursor-not-allowed disabled:opacity-50">{linked?"Portal Active":busy?"Sending...":invited?"Resend Invite":"Send Invite"}</button>{linked?<span className="text-xs font-semibold text-[#647d5b]">Client portal connected</span>:message?<span className="text-xs font-semibold text-[#647d5b]">{message}</span>:null}</div>}
