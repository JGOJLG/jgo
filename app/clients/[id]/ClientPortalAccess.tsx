"use client";
import{useState}from"react";
type Props={clientId:number;clientName:string;email:string|null;linked:boolean;invitedAt:string|null};
export default function ClientPortalAccess({clientId,clientName,email,linked,invitedAt}:Props){const[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[invited,setInvited]=useState(Boolean(invitedAt));async function invite(){if(!email){setMessage("Add an email address first.");return}setBusy(true);setMessage("");try{const r=await fetch("/api/client-portal/invite",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({clientId})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to send invite.");setMessage(d.message||"Invite sent.");setInvited(true)}catch(e){setMessage(e instanceof Error?e.message:"Unable to send invite.")}finally{setBusy(false)}}const status=linked?"Active":invited?"Invite Sent":"Not Invited";return <section className="h-full rounded-2xl border border-[#dfe6db] bg-white p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#7f9975]">Client Portal</p><h2 className="mt-1 text-lg font-bold text-[#243128]">Portal Access</h2></div><span className="rounded-full bg-[#eef3ea] px-2.5 py-1 text-[11px] font-bold text-[#4d6247]">{status}</span></div><p className="mt-3 text-sm text-[#708075]">{linked?`${clientName}'s account is connected.`:invited?`Invite sent to ${email}.`:`Send ${clientName} access to documents, resources, and the job tracker.`}</p><button type="button" onClick={invite} disabled={busy||linked||!email} className="mt-4 w-full rounded-xl bg-[#647d5b] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{linked?"Portal Connected":busy?"Sending...":invited?"Resend Invite":"Send Portal Invite"}</button>{message?<p className="mt-2 text-xs font-semibold text-[#4d6247]">{message}</p>:null}</section>}
