"use client";
import Link from"next/link";import{archiveClient,deleteClientPermanently,restoreClient}from"./actions";
type Props={clientId:number;clientName:string;isArchived:boolean};
export default function ClientActions({clientId,clientName,isArchived}:Props){function confirmDelete(e:React.FormEvent<HTMLFormElement>){if(!window.confirm(`Delete ${clientName} permanently? This cannot be undone.`))e.preventDefault()}return <div className="flex flex-wrap gap-3"><Link href={`/clients/${clientId}/edit`} className="rounded-xl border border-[#cbd8c4] bg-white px-4 py-2.5 text-sm font-semibold text-[#4d6247] hover:bg-[#f5f7f2]">Edit Client Profile</Link>{isArchived?<form action={restoreClient}><input type="hidden" name="clientId" value={clientId}/><button className="rounded-xl border border-[#cbd8c4] bg-white px-4 py-2.5 text-sm font-semibold text-[#4d6247]">Restore Client</button></form>:<form action={archiveClient}><input type="hidden" name="clientId" value={clientId}/><button className="rounded-xl border border-[#d8cdb8] bg-white px-4 py-2.5 text-sm font-semibold text-[#80633b]">Archive Client</button></form>}<form action={deleteClientPermanently} onSubmit={confirmDelete}><input type="hidden" name="clientId" value={clientId}/><button className="rounded-xl border border-[#e2c6c2] bg-white px-4 py-2.5 text-sm font-semibold text-[#9a554d]">Delete Permanently</button></form></div>}
