"use client";

import { useMemo, useState } from "react";

declare global { interface Window { jspdf?: any } }

type Change = { title: string; body: string };
type ReportData = { client:string; overallFeedback:string; changes:Change[]; recruiterPerspective:string; futureUpdates:string[]; finalRecommendation:string };
const emptyReport:ReportData={client:"",overallFeedback:"",changes:[],recruiterPerspective:"",futureUpdates:[],finalRecommendation:""};

const STANDARD_RECOMMENDATIONS=[
 (client:string)=>`Save your resume as a PDF named "${client||"First Last"} Resume.pdf." Unless an employer specifically requests a Word document, submit the PDF version to preserve formatting.`,
 ()=>"Add your LinkedIn profile URL to your resume after updating your profile.",
 ()=>"Update your LinkedIn profile so it matches your new resume, including your headline, About section, experience, and skills. Find more at jgohire.com/guide.",
 ()=>"Tailor your Professional Summary for each position by incorporating the most important keywords and priorities from the job description.",
 ()=>"Customize your resume when appropriate by adding relevant keywords from each posting to strengthen ATS alignment.",
 ()=>"Submit a tailored cover letter whenever possible. Use it to connect your background, motivation, and most relevant experience directly to the opportunity.",
 ()=>"Review the PDF before applying to make sure all formatting, spacing, links, and page breaks appear correctly.",
 ()=>"Prepare interview examples that reinforce the strongest accomplishments and leadership themes highlighted in your resume.",
 ()=>"Practice your career story so you can clearly explain where you have been, what you do best, and why this opportunity makes sense as your next step.",
 ()=>"Apply early and reach out directly. Whenever possible, apply within the first few days, identify the hiring manager or recruiter, and make a thoughtful connection on LinkedIn or by email.",
];

function clean(v:string){return v.replace(/^#{1,6}\s*/,"").replace(/^\*\*(.*?)\*\*$/,"$1").replace(/\*\*/g,"").trim()}
function cleanBullet(v:string){return clean(v).replace(/^[•●▪◦\-*]\s*/,"").trim()}
function splitLines(v:string){return v.split(/\n+/).map(cleanBullet).filter(Boolean)}
function fileBaseName(name:string){const x=name.replace(/[^a-zA-Z0-9 .'-]/g,"").replace(/\s+/g," ").trim();return `${x||"JGO Hire"} Resume Ready Report`}
function normalizeTag(line:string){const m=line.trim().match(/^\[\/?([A-Z0-9_ &-]+)\]$/i);return m?m[1].toUpperCase().replace(/[ &-]+/g,"_"):null}
function getTaggedBlocks(raw:string){const blocks:{tag:string;value:string}[]=[];let tag="",buf:string[]=[];const flush=()=>{if(tag)blocks.push({tag,value:buf.join("\n").trim()});buf=[]};for(const line of raw.replace(/\r/g,"").split("\n")){const t=normalizeTag(line);if(t){if(t==="JGO_REPORT")continue;flush();tag=t}else if(tag)buf.push(line)}flush();return blocks}

function parseStructured(raw:string):ReportData|null{
 if(!/\[JGO_REPORT\]/i.test(raw)&&!/\[OVERALL_FEEDBACK\]/i.test(raw))return null;
 const out={...emptyReport,changes:[],futureUpdates:[]} as ReportData;let current:Partial<Change>|null=null;let before="",after="",legacyNow="",legacyPositioning="";let legacySelling:string[]=[];
 const push=()=>{if(current&&(current.title||current.body))out.changes.push({title:current.title||"Resume Improvement",body:current.body||""});current=null};
 for(const b of getTaggedBlocks(raw)){const v=b.value.trim();switch(b.tag){case"CLIENT":out.client=v;break;case"OVERALL_FEEDBACK":out.overallFeedback=v;break;case"CHANGE":push();current={};break;case"TITLE":if(current)current.title=v;break;case"BODY":if(current)current.body=v;break;case"RECRUITER_PERSPECTIVE":out.recruiterPerspective=v;break;case"FUTURE_UPDATES":out.futureUpdates=splitLines(v);break;case"FINAL_RECOMMENDATION":out.finalRecommendation=v;break;case"TRANSFORMATION_BEFORE":before=v;break;case"TRANSFORMATION_AFTER":after=v;break;case"BIGGEST_DIFFERENCE_NOW":legacyNow=v;break;case"POSITIONING":legacyPositioning=v;break;case"SELLING_POINTS":legacySelling=splitLines(v);break}}push();
 if(!out.overallFeedback&&(before||after))out.overallFeedback=[before,after].filter(Boolean).join("\n\n");if(!out.recruiterPerspective)out.recruiterPerspective=legacyNow;if(!out.futureUpdates.length)out.futureUpdates=legacySelling.slice(0,6);if(!out.finalRecommendation)out.finalRecommendation=legacyPositioning;return out;
}

function headingKey(line:string){return clean(line).replace(/[’]/g,"'").replace(/:$/,"").toUpperCase()}
function sectionRange(lines:string[],heading:string,next:string[]){const start=lines.findIndex(l=>headingKey(l)===heading);if(start<0)return[];let end=lines.length;for(let i=start+1;i<lines.length;i++){if(next.includes(headingKey(lines[i]))){end=i;break}}return lines.slice(start+1,end)}
function joinParagraphs(lines:string[]){const parts:string[]=[];let buf:string[]=[];const flush=()=>{if(buf.length){parts.push(buf.join(" ").replace(/\s+/g," ").trim());buf=[]}};for(const raw of lines){const line=clean(raw);if(!line){flush();continue}buf.push(line)}flush();return parts.join("\n\n")}
function parseChanges(lines:string[]){const changes:Change[]=[];let title="",body:string[]=[];const flush=()=>{if(title){const text=body.map(cleanBullet).filter(Boolean).join(" ").replace(/\s+/g," ").trim();if(text)changes.push({title,body:text})}title="";body=[]};for(let i=0;i<lines.length;i++){const raw=lines[i];const line=clean(raw);if(!line)continue;const next=lines.slice(i+1).find(x=>clean(x));const isBullet=/^[•●▪◦\-*]\s*/.test(raw.trim());const nextIsBullet=!!next&&/^[•●▪◦\-*]\s*/.test(next.trim());if(!isBullet&&nextIsBullet){flush();title=line;continue}if(!title&&!isBullet){title=line;continue}body.push(raw)}flush();return changes}
function parseFuture(lines:string[]){const items:string[]=[];let current="";for(const raw of lines){const line=clean(raw);if(!line)continue;const bulletStart=/^[•●▪◦\-*]\s*/.test(raw.trim());if(bulletStart){if(current)items.push(current.trim());current=cleanBullet(raw)}else if(current){current+=` ${line}`}else if(!/^As your career|^As your role/i.test(line)){current=line}}if(current)items.push(current.trim());return items.filter(Boolean)}

function parsePlain(raw:string):ReportData{
 const out={...emptyReport,changes:[],futureUpdates:[]} as ReportData;const lines=raw.replace(/\r/g,"").split("\n");const nonEmpty=lines.map(clean).filter(Boolean);
 const titleIndex=nonEmpty.findIndex(l=>/JGO HIRE RESUME READY REPORT/i.test(l));if(titleIndex>=0){for(let i=titleIndex+1;i<nonEmpty.length;i++){const l=nonEmpty[i];if(/^Prepared by/i.test(l))continue;if(!/^(Overall Feedback|What I Changed|Recruiter's Perspective|Recruiters Perspective|Suggestions for Future Resume Updates)/i.test(l)){out.client=l;break}}}
 const clientLine=nonEmpty.find(l=>/^CLIENT\s*:/i.test(l)||/^PREPARED FOR\s+/i.test(l));if(clientLine)out.client=clientLine.replace(/^CLIENT\s*:/i,"").replace(/^PREPARED FOR\s+/i,"").trim();
 const overall=sectionRange(lines,"OVERALL FEEDBACK",["WHAT I CHANGED","RECRUITER'S PERSPECTIVE","RECRUITERS PERSPECTIVE","SUGGESTIONS FOR FUTURE RESUME UPDATES"]);out.overallFeedback=joinParagraphs(overall);
 const changes=sectionRange(lines,"WHAT I CHANGED",["RECRUITER'S PERSPECTIVE","RECRUITERS PERSPECTIVE","SUGGESTIONS FOR FUTURE RESUME UPDATES"]);out.changes=parseChanges(changes);
 let perspective=sectionRange(lines,"RECRUITER'S PERSPECTIVE",["SUGGESTIONS FOR FUTURE RESUME UPDATES"]);if(!perspective.length)perspective=sectionRange(lines,"RECRUITERS PERSPECTIVE",["SUGGESTIONS FOR FUTURE RESUME UPDATES"]);out.recruiterPerspective=joinParagraphs(perspective);
 const futureStart=lines.findIndex(l=>headingKey(l)==="SUGGESTIONS FOR FUTURE RESUME UPDATES");if(futureStart>=0){const after=lines.slice(futureStart+1);const finalAt=after.findIndex(l=>/^\s*Final Recommendation\s*:/i.test(clean(l)));const futureLines=finalAt>=0?after.slice(0,finalAt):after;out.futureUpdates=parseFuture(futureLines);if(finalAt>=0){const first=clean(after[finalAt]).replace(/^Final Recommendation\s*:\s*/i,"");const rest=after.slice(finalAt+1).filter(l=>!/^\s*JGO Hire Recommendations/i.test(clean(l)));out.finalRecommendation=[first,...rest].map(clean).filter(Boolean).join(" ").replace(/\s+/g," ").trim()}}
 if(!out.finalRecommendation){const finalLine=lines.findIndex(l=>/^\s*Final Recommendation\s*:/i.test(clean(l)));if(finalLine>=0)out.finalRecommendation=lines.slice(finalLine).map(clean).join(" ").replace(/^Final Recommendation\s*:\s*/i,"").trim()}
 return out;
}
function parseImport(raw:string){return parseStructured(raw)||parsePlain(raw)}

function Field({label,value,onChange,rows=5}:{label:string;value:string;onChange:(v:string)=>void;rows?:number}){return <label className="block"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[0.16em] text-[#7b8779]">{label}</span><textarea value={value} onChange={e=>onChange(e.target.value)} rows={rows} className="w-full resize-y rounded-xl border border-[#e0e5dc] bg-white px-4 py-3 text-sm leading-6 text-[#343b33] outline-none transition focus:border-[#aab6a6] focus:ring-2 focus:ring-[#e7ece4]"/></label>}
function Input({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[0.16em] text-[#7b8779]">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-xl border border-[#e0e5dc] bg-white px-4 py-3 text-sm text-[#343b33] outline-none transition focus:border-[#aab6a6] focus:ring-2 focus:ring-[#e7ece4]"/></label>}
function loadScript(src:string,id:string){return new Promise<void>((resolve,reject)=>{const existing=document.getElementById(id) as HTMLScriptElement|null;if(existing){if(existing.dataset.loaded==="true")return resolve();existing.addEventListener("load",()=>resolve(),{once:true});existing.addEventListener("error",()=>reject(new Error("Could not load PDF tools.")),{once:true});return}const script=document.createElement("script");script.id=id;script.src=src;script.async=true;script.onload=()=>{script.dataset.loaded="true";resolve()};script.onerror=()=>reject(new Error("Could not load PDF tools."));document.head.appendChild(script)})}
async function imageData(url:string){const blob=await fetch(url).then(r=>{if(!r.ok)throw new Error("Logo not found");return r.blob()});return await new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=reject;reader.readAsDataURL(blob)})}

async function makePdf(data:ReportData){
 await loadScript("https://cdn.jsdelivr.net/npm/jspdf@2.5.2/dist/jspdf.umd.min.js","jgo-report-jspdf");const jsPDF=window.jspdf?.jsPDF;if(!jsPDF)throw new Error("PDF generator did not load.");const d=new jsPDF({unit:"pt",format:"letter"});const W=612,H=792,M=40,CW=W-M*2,TOP=48,BOTTOM=742;const black:[number,number,number]=[22,22,22],gray:[number,number,number]=[95,95,95];let y=TOP;
 const font=(size:number,bold=false,color=black)=>{d.setFont("helvetica",bold?"bold":"normal");d.setFontSize(size);d.setTextColor(...color)};const wrap=(text:string,width:number,size=9.1,bold=false)=>{font(size,bold);return d.splitTextToSize(text,width) as string[]};const addPage=()=>{d.addPage();y=TOP};const ensure=(h:number)=>{if(y+h>BOTTOM)addPage()};
 const paragraph=(text:string,size=9.2,bold=false,gap=11)=>{if(!text.trim())return;for(const para of text.split(/\n\s*\n/).map(p=>p.replace(/\s+/g," ").trim()).filter(Boolean)){const lines=wrap(para,CW,size,bold),h=lines.length*size*1.34;ensure(h+gap);font(size,bold);d.text(lines,M,y);y+=h+gap}};const heading=(title:string)=>{ensure(28);y+=4;font(11.2,true);d.text(title,M,y);y+=16};const subheading=(title:string)=>{ensure(22);font(9.3,true);d.text(title,M,y);y+=14};const bullet=(text:string,size=9)=>{const lines=wrap(text.replace(/\s+/g," ").trim(),CW-38,size,false),h=lines.length*size*1.32+7;ensure(h);font(size,false);d.circle(M+19,y-3,1.45,"F");d.text(lines,M+34,y);y+=h};
 try{const logo=await imageData("/jgo-hire-logo.png");d.addImage(logo,"PNG",W-M-78,16,74,29)}catch{}font(15.5,true);d.text("JGO Hire Resume Ready Report",W/2,45,{align:"center"});font(7.5,true);d.text("TM",W/2+107,39);font(10.5,false);d.text(data.client||"Client Name",W/2,64,{align:"center"});font(8.5,false);d.text("Prepared by Jen Gordon | Certified Career Coach & Recruiter",W/2,80,{align:"center"});y=103;
 heading("Overall Feedback");paragraph(data.overallFeedback);heading("What I Changed");data.changes.forEach(c=>{subheading(c.title);bullet(c.body);y+=2});heading("Recruiter's Perspective");paragraph(data.recruiterPerspective);heading("Suggestions for Future Resume Updates");paragraph("As your career continues to grow, keep track of measurable accomplishments that can strengthen future versions of your resume, including:",9.2,false,9);data.futureUpdates.forEach(i=>bullet(i));ensure(48);font(9.2,true);d.text("Final Recommendation:",M,y);const rec=wrap(data.finalRecommendation,CW-105,9.2,false);font(9.2,false);d.text(rec,M+105,y);y+=Math.max(18,rec.length*12.2)+16;
 heading("JGO Hire Recommendations");paragraph("Before submitting your application, complete the following steps to maximize your chances of landing an interview:",9.2,false,9);STANDARD_RECOMMENDATIONS.forEach(f=>bullet(f(data.client),8.9));heading("JGO Hire Pro Tip");paragraph("A great resume should answer three questions within the first 30 to 60 seconds:",9.2,false,7);["Can you do the job?","Why are you a strong fit for this position?","Why should the employer interview you?"].forEach((q,i)=>{ensure(18);font(9.2,false);d.text(`${i+1}. ${q}`,M+13,y);y+=15});y+=4;paragraph("Your updated resume was intentionally designed to answer those questions by making your most relevant experience, accomplishments, leadership, and value easier to see.",9.2,false,0);const pages=d.getNumberOfPages();for(let p=1;p<=pages;p++){d.setPage(p);font(6.5,false,gray);d.text(`JGO HIRE | RESUME READY REPORT | ${p} OF ${pages}`,W/2,H-18,{align:"center"})}d.save(`${fileBaseName(data.client)}.pdf`)
}

export default function ResumeReadyReportPage(){
 const[report,setReport]=useState<ReportData>(emptyReport),[importText,setImportText]=useState(""),[status,setStatus]=useState("");const completion=useMemo(()=>{const checks=[report.client,report.overallFeedback,report.changes.length,report.recruiterPerspective,report.futureUpdates.length,report.finalRecommendation];return Math.round(checks.filter(Boolean).length/checks.length*100)},[report]);const update=(key:keyof ReportData,value:any)=>setReport(r=>({...r,[key]:value}));
 const doImport=()=>{try{const parsed=parseImport(importText);const found=[parsed.client,parsed.overallFeedback,parsed.changes.length,parsed.recruiterPerspective,parsed.futureUpdates.length,parsed.finalRecommendation].filter(Boolean).length;if(!found){setStatus("I couldn't recognize that report. Paste the full report including the section headings.");return}setReport(parsed);setStatus(parsed.client?`Formatted successfully for ${parsed.client}. Review the fields on the right.`:"Formatted successfully. Add the client name, then review the fields on the right.")}catch(e:any){setStatus(e?.message||"Could not format this report.")}};
 const download=async()=>{try{setStatus("Building JGO Hire Resume Ready Report...");await makePdf(report);setStatus("Report downloaded.")}catch(e:any){setStatus(e?.message||"Could not create PDF.")}};
 return <main className="min-h-screen bg-[#f6f7f4] px-4 py-8 text-[#30372f] md:px-8 md:py-10"><div className="mx-auto max-w-[1320px]"><div className="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-[#7e8a7b]">JGO Hire</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.03em] text-[#2e352d]">Resume Ready Report</h1><p className="mt-2 max-w-2xl text-sm text-[#7a8378]">Personalized recruiter feedback first. Standard JGO Hire recommendations automatically added to every report.</p></div><div className="flex items-center gap-3 rounded-full border border-[#e1e5dd] bg-white px-4 py-2 shadow-sm"><div className="h-1.5 w-20 overflow-hidden rounded-full bg-[#edf0eb]"><div className="h-full rounded-full bg-[#758a70]" style={{width:`${completion}%`}}/></div><span className="text-xs font-semibold text-[#596656]">{completion}% ready</span></div></div><div className="grid gap-6 xl:grid-cols-[390px_1fr]"><aside className="space-y-4"><section className="rounded-2xl border border-[#e1e5dd] bg-white p-5 shadow-[0_8px_30px_rgba(51,61,48,.04)]"><h2 className="text-base font-semibold">Quick Paste</h2><p className="mt-1 text-xs leading-5 text-[#7d867b]">Paste the complete JGO report from ChatGPT. The builder recognizes both the tagged format and the plain report format with section headings.</p><textarea value={importText} onChange={e=>setImportText(e.target.value)} rows={23} className="mt-4 w-full resize-y rounded-xl border border-[#e0e5dc] bg-[#fafbf9] px-4 py-3 text-sm leading-6 outline-none transition focus:border-[#aab6a6] focus:ring-2 focus:ring-[#e7ece4]" placeholder="Paste report here..."/><button type="button" onClick={doImport} disabled={!importText.trim()} className="mt-3 w-full rounded-xl bg-[#4f614b] px-4 py-3 text-sm font-semibold text-white transition hover:bg-[#445441] disabled:opacity-40">Format Report</button>{status&&<p className="mt-3 rounded-lg bg-[#f3f6f1] px-3 py-2 text-xs leading-5 text-[#566452]">{status}</p>}</section><section className="rounded-2xl border border-[#e1e5dd] bg-white p-5"><p className="text-[10px] font-semibold uppercase tracking-[0.15em] text-[#7b8779]">Always Included</p><p className="mt-2 text-sm font-semibold">JGO Hire Recommendations + Pro Tip</p><p className="mt-1 text-xs leading-5 text-[#7d867b]">These sections are standardized and automatically added to every client report.</p></section></aside><section className="rounded-2xl border border-[#e1e5dd] bg-white p-6 shadow-[0_8px_30px_rgba(51,61,48,.04)] md:p-7"><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-base font-semibold">Review & Edit</h2><p className="mt-1 text-xs text-[#7d867b]">This is the personalized portion of the report.</p></div><button type="button" onClick={download} disabled={!report.client} className="rounded-xl bg-[#4f614b] px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-[#445441] disabled:opacity-40">Download PDF</button></div><Input label="Client" value={report.client} onChange={v=>update("client",v)}/><div className="mt-6"><Field label="Overall Feedback" value={report.overallFeedback} onChange={v=>update("overallFeedback",v)} rows={8}/></div><div className="mt-7 border-t border-[#edf0ea] pt-6"><div className="mb-4 flex items-center justify-between"><h3 className="text-sm font-semibold">What I Changed</h3><button type="button" onClick={()=>update("changes",[...report.changes,{title:"",body:""}])} className="text-xs font-semibold text-[#60705d]">+ Add Change</button></div><div className="space-y-3">{report.changes.map((c,i)=><div key={i} className="rounded-xl bg-[#fafbf9] p-4"><Input label={`Change ${i+1} Title`} value={c.title} onChange={v=>{const n=[...report.changes];n[i]={...n[i],title:v};update("changes",n)}}/><div className="mt-3"><Field label="Explanation" value={c.body} onChange={v=>{const n=[...report.changes];n[i]={...n[i],body:v};update("changes",n)}} rows={4}/></div></div>)}</div></div><div className="mt-7 border-t border-[#edf0ea] pt-6"><Field label="Recruiter's Perspective" value={report.recruiterPerspective} onChange={v=>update("recruiterPerspective",v)} rows={7}/></div><div className="mt-7 border-t border-[#edf0ea] pt-6"><Field label="Suggestions for Future Resume Updates - one per line" value={report.futureUpdates.join("\n")} onChange={v=>update("futureUpdates",splitLines(v))} rows={8}/></div><div className="mt-7 border-t border-[#edf0ea] pt-6"><Field label="Final Recommendation" value={report.finalRecommendation} onChange={v=>update("finalRecommendation",v)} rows={5}/></div></section></div></div></main>
}
